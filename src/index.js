import { mul, div, sub, add } from "./math";
// const { mul, div, sub, add } = require('./math');

console.log(`multiple: ${mul(10, 3)}`);
console.log(`divide: ${div(10, 3)}`);
console.log(`addition: ${add(10, 3)}`);
console.log(`subtract: ${sub(10, 3)}`);
