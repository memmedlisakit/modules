const mul = (a, b) => a * b;
const div = (a, b) => a / b;
const add = (a, b) => a + b;
const sub = (a, b) => a - b;

export { mul, div, add, sub };

// module.exports = {
//     mul, div, add, sub
// }
